#' @@title quad
#'
#' @param x, a, b, c - params for the quatric function y=mx+b
#'
#' @description Returns the result of the function y=mx+b with given params. Just for a lab assigned exmaple
#' @return Number Value
#'
quad = function(x, a, b, c) {
  return(a * x^2 + b * x + c)
}
